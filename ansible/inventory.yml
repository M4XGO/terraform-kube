---
# Inventory local — Ansible s'exécute sur la machine hôte et pilote
# le cluster via kubectl / l'API Kubernetes.

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
