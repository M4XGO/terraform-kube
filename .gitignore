# ─── Terraform ────────────────────────────────────────────────────────────────
terraform/k8s/.terraform/
terraform/k8s/.terraform.lock.hcl
terraform/k8s/.terraform-helm/

# State (dossier dédié)
state/terraform.tfstate
state/*.backup

# Outputs générés
terraform-outputs.json
vm-outputs.json
vm-ids.json

# Anciens emplacements (sécurité)
*.tfstate
*.tfstate.backup
.terraform/
.terraform-helm/
.claude/
