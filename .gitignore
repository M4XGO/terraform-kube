# ─── Terraform ────────────────────────────────────────────────────────────────
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/.terraform-helm/

# State (dossier dédié)
state/terraform.tfstate
state/*.backup

# Outputs générés
terraform-outputs.json

# Anciens emplacements (sécurité)
*.tfstate
*.tfstate.backup
.terraform/
.terraform-helm/
